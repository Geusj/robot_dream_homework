#1. Написати sql запит, який вибере усі записи із таблиці users старше 30 років.


    SELECT * FROM users WHERE age > 30


#2. Написати sql запит, який виведе кількість записів в табліці users, що старше 30 років.


    SELECT COUNT(*) FROM users WHERE age > 30


#3. Написати sql запит, який виведе інформацію про вік (кількість років) та кількість користувачів,
# які відповідають цьому віку. Результатом виконання такого запиту має бути таблиця:



    SELECT age, COUNT(*) as users
        FROM users
        GROUP BY age


#4. Написати sql запит, який буде робити те саме, що і в завданні 3,
#але виводити дані відсортовані за кількістю користувачів у спадаючому порядку та по віку у зростаючому порядку.


    SELECT age, COUNT(*) AS users
        FROM users
        GROUP BY age
        ORDER BY users DESC, age ASC


#5. (необов'язкове виконання) Модифікувати попередній запит таким чином,
щоб з отриманого результату вибрати тільки ті записи, де значення users більше 1. Результатом має бути таблиця:


    SELECT age, users
    FROM (
        SELECT age, COUNT(*) AS users
        FROM users
        GROUP BY age
        ORDER BY users DESC, age ASC
    ) AS subquery
    WHERE users > 1